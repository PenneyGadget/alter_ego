<h1> <%= @team.name %> Teammates </h1>

<% @users.all do |user| %>

  <%= image_tag Giphy.random(user.gif_link).image_original_url.to_s %>

  <%= link_to user.name, team_user_path(user) %>

<% end %>


<% if current_admin? %>
  <%= link_to "Edit Teammates", team_admin_users %>

<% else %>
